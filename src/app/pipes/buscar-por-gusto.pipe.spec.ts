import { BuscarPorGustoPipe } from './buscar-por-gusto.pipe';

describe('BuscarPorGustoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarPorGustoPipe();
    expect(pipe).toBeTruthy();
  });
});
